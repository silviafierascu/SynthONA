% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{synthona_params}
\alias{synthona_params}
\title{Specify the parameters of a synthetic organisational network}
\usage{
synthona_params(
  n = 500L,
  template = "tech_product",
  topology = "hierarchy",
  mean_degree = 12,
  within_share = 0.65,
  layers = c("communication", "advice", "trust"),
  snapshots = "snapshot",
  snapshot_mode = c("cumulative", "alternative"),
  seed_topology = SEED_TOPOLOGY_BASE,
  seed_attributes = SEED_ATTRIBUTE_BASE,
  scenario_id = NULL,
  label = NULL,
  extras = list()
)
}
\arguments{
\item{n}{Number of employees. At least 20.}

\item{template}{Industry template identifier, see \code{\link[=synthona_templates]{synthona_templates()}}.}

\item{topology}{Base structure. One of \code{"hierarchy"}, \code{"sbm"},
\code{"sbm_dual_legacy"}, \code{"er"}, \code{"ws"} or \code{"ba"}.}

\item{mean_degree}{Target average degree of the base structure.}

\item{within_share}{Proportion of the ties of a person that fall inside
their own department, between 0 and 1. Higher values produce more
strongly siloed organisations.}

\item{layers}{Relational layers to generate, see \link{SYNTHONA_LAYERS}.}

\item{snapshots}{Character vector naming the snapshots to produce. A single
value generates a cross-section; several values generate a panel.}

\item{snapshot_mode}{Either \code{"cumulative"}, where each snapshot evolves from
the previous one, or \code{"alternative"}, where each snapshot is an
independent variant of the same baseline. Use \code{"alternative"} for
what-if comparisons such as competing reorganisation options.}

\item{seed_topology}{Integer seed for structural randomness.}

\item{seed_attributes}{Integer seed for attribute randomness. Holding this
fixed while varying \code{seed_topology} produces structural variants of the
same workforce.}

\item{scenario_id}{Optional scenario identifier, set when the parameters
come from \code{\link[=synthona_scenario]{synthona_scenario()}}.}

\item{label}{Optional human-readable label.}

\item{extras}{Named list of scenario-specific settings.}
}
\value{
An object of class \code{synthona_params}.
}
\description{
Builds and validates the parameter object that drives every generator in
the protocol. A \code{synthona_params} object is a complete, inspectable
description of a dataset: given the same object, \code{\link[=synthona_generate]{synthona_generate()}}
reproduces the same network exactly.
}
\section{Calibration on mean degree}{

Tie volume is specified as \code{mean_degree}, the average number of contacts a
person has, rather than as a tie probability. Fixing a probability makes
density constant and therefore forces average degree to grow in proportion
to headcount, so a 1500-person organisation would come out roughly fifteen
times better connected per capita than a 100-person one. Real organisations
do not behave that way: individual contact volume is bounded by time and
attention, so degree stays broadly flat while density falls. Calibrating on
degree keeps datasets comparable across sizes, which is what makes a
benchmark corpus usable for method comparison.

\code{mean_degree} is the target for the \emph{base structure}. Each relational layer
is a thinned view of it, so a single layer has lower degree than the target
(communication retains about 85\% of ties, mentorship about 25\%), while the
union across several layers approaches it. Use \code{\link[=validate_dataset]{validate_dataset()}} to see
the realised figure for any specification.
}

\examples{
p <- synthona_params(n = 300, topology = "sbm", mean_degree = 14)
p

# A more strongly siloed organisation of the same size and tie volume
silos <- synthona_params(n = 300, topology = "sbm", mean_degree = 14,
                         within_share = 0.9)
silos$within_share
}
