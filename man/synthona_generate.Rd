% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{synthona_generate}
\alias{synthona_generate}
\title{Generate a synthetic organisational network dataset}
\usage{
synthona_generate(params, non_human_actors = 0L)
}
\arguments{
\item{params}{A \code{\link[=synthona_params]{synthona_params()}} object, or a scenario identifier
accepted by \code{\link[=synthona_scenario]{synthona_scenario()}}.}

\item{non_human_actors}{Number of non-human actors (AI agents, bots, shared
accounts) to add to the workforce.}
}
\value{
An object of class \code{synthona_dataset}, a list with \code{nodes}, \code{edges},
\code{truth} and \code{params}.
}
\description{
Runs the full protocol for one parameter specification: draws a workforce,
builds the base structure, derives the relational layers, evolves any
requested snapshots, and records the ground truth.
}
\details{
The returned object is the complete dataset. Applying survey measurement
error to it is a separate step, \code{\link[=synthona_observe]{synthona_observe()}}, so that the same true
network can be observed under many different survey designs.
}
\examples{
d <- synthona_generate(synthona_params(n = 150, topology = "sbm"))
d
head(d$nodes[, c("person_id", "department", "role_bucket")])

# The same specification always reproduces the same dataset
identical(d$edges, synthona_generate(d$params)$edges)
}
