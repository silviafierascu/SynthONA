% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{synthona_write}
\alias{synthona_write}
\title{Write a dataset to disk}
\usage{
synthona_write(
  dataset,
  output_root = default_output_dir(),
  graphml = TRUE,
  metrics = TRUE
)
}
\arguments{
\item{dataset}{A \code{synthona_dataset}.}

\item{output_root}{Directory to write into.}

\item{graphml}{Whether to also write a GraphML file.}

\item{metrics}{Whether to compute and write metric tables.}
}
\value{
The path written to, invisibly.
}
\description{
Writes the node table, the edge table (whole, per layer and per snapshot),
the computed metrics, the ground truth and a manifest. The manifest records
the full parameter specification and the protocol version, so any exported
dataset can be regenerated exactly from the files alone.
}
\details{
Nothing is written outside \code{output_root}, which defaults to a session
temporary directory.
}
\examples{
d <- synthona_generate(synthona_params(n = 60))
path <- synthona_write(d, file.path(tempdir(), "demo"))
list.files(path)
}
