% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observe.R
\name{synthona_observe}
\alias{synthona_observe}
\title{Observe a generated network through a survey design}
\usage{
synthona_observe(dataset, design = observation_design(), seed = NULL)
}
\arguments{
\item{dataset}{A \code{synthona_dataset} from \code{\link[=synthona_generate]{synthona_generate()}}.}

\item{design}{An \code{\link[=observation_design]{observation_design()}}, or \code{NULL} for a census.}

\item{seed}{Integer seed for the measurement draws.}
}
\value{
A \code{synthona_dataset} whose \code{edges} are the observed ties, carrying
an \code{observation} element recording what was applied. The \code{truth} element
is preserved unchanged.
}
\description{
Applies non-response, name-generator truncation and recall bias to a
complete network, returning what a survey would actually have captured. The
underlying dataset is left untouched, so the same true network can be
observed repeatedly under different designs.
}
\examples{
d <- synthona_generate(synthona_params(n = 200))
obs <- synthona_observe(d, observation_design(response_rate = 0.6,
                                              name_generator_limit = 5))
nrow(d$edges)
nrow(obs$edges)
}
