% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/topology.R
\name{generate_base_structure}
\alias{generate_base_structure}
\alias{generate_random_graph}
\alias{generate_small_world}
\alias{generate_scale_free}
\alias{generate_sbm}
\alias{generate_hierarchy}
\title{Generate the base organisational structure}
\usage{
generate_base_structure(nodes, params)

generate_random_graph(nodes, params)

generate_small_world(nodes, params)

generate_scale_free(nodes, params)

generate_sbm(nodes, params, dual_legacy = FALSE)

generate_hierarchy(nodes, params)
}
\arguments{
\item{nodes}{A node table from \code{\link[=generate_profiles]{generate_profiles()}}.}

\item{params}{A \code{\link[=synthona_params]{synthona_params()}} object.}

\item{dual_legacy}{Whether to plant two legacy organisations, as in a
post-merger integration.}
}
\value{
A list with \code{graph}, \code{nodes} and \code{truth}.
}
\description{
Dispatches on \code{params$topology} and returns both the graph and the node
table in the vertex order actually used, together with the ground truth the
structure was built from.
}
\examples{
p <- synthona_params(n = 120, topology = "sbm")
base <- generate_base_structure(generate_profiles(p), p)
igraph::vcount(base$graph)
base$truth$model
}
